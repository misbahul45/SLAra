# SLAra dashboard — dev config template. Copy to `.env` (gitignored).
# Mock-first: "true" serves local JSON fixtures with simulated latency.
# Flip to "false" on H-1 to hit the real FastAPI `ai` service.
VITE_USE_MOCK=true
VITE_API_BASE_URL=http://localhost:8000/api/v1
